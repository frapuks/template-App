// nom avec majuscule : Fxample
// Modifier le nom des colonnes
export type Fxample = {
  id?: number;
  first_name: string;
  last_name: string;
  is_goalkeeper: boolean;
};

export interface FxampleSchema {
  first_name: string;
  last_name: string;
  is_goalkeeper: boolean;
}