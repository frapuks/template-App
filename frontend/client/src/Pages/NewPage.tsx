import {
  Typography,
  Container,
} from "@mui/material";

const NewPage = () => {
  // Utils

  // Variables

  // UseEffect

  // Methods

  return (
    <Container sx={{ padding: 0 }}>
      <Typography variant="h5" textAlign={"center"}>
        NewPage
      </Typography>
    </Container>
  );
};

export default NewPage;
