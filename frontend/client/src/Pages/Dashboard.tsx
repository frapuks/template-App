import {
  Typography,
  Container,
} from "@mui/material";

const Dashboard = () => {
  // Utils

  // Variables

  // UseEffect

  // Methods

  return (
    <Container sx={{ padding: 0 }}>
      <Typography variant="h5" textAlign={"center"}>
        Dashboard
      </Typography>
    </Container>
  );
};

export default Dashboard;
